import { describe, test } from "vitest";

// Note: Data layer tests typically require a test database or mocking drizzle
// This is a placeholder showing the structure

describe("expense data layer", () => {
  test.skip("getForTab returns expenses with splits", async () => {
    // Would need test DB setup
    // const result = await expense.getForTab('tab1')
    // expect(result.expenses).toBeDefined()
  });
});
