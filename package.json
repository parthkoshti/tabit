{
  "name": "tabit",
  "private": true,
  "scripts": {
    "build": "turbo build --concurrency=1 --filter=!web",
    "build:app": "turbo build --concurrency=1 --filter=api --filter=pwa --filter=workers",
    "build:data": "turbo build --filter=data",
    "build:web": "cd apps/web && pnpm build",
    "generate-icons": "node scripts/generate-icons.js",
    "generate-https-certs": "node scripts/generate-https-certs.js",
    "dev": "pnpm dev:app",
    "dev:app": "turbo dev --filter=api --filter=pwa --filter=workers",
    "dev:web": "cd apps/web && pnpm dev",
    "dev:pwa": "turbo dev --filter=pwa",
    "start": "pnpm --filter=pwa start",
    "start:web": "cd apps/web && pnpm start",
    "start:pwa": "pnpm --filter=pwa start",
    "start:api": "pnpm --filter=api start",
    "start:workers": "pnpm --filter=workers start",
    "start:all": "concurrently -n api,workers,web,pwa -c blue,green,magenta,yellow \"pnpm --filter=api start\" \"pnpm --filter=workers start\" \"cd apps/web && pnpm start\" \"pnpm --filter=pwa start\"",
    "start:prod": "pnpm --filter=db db:migrate:prod && concurrently -n api,workers,web,pwa -c blue,green,magenta,yellow \"pnpm --filter=api start:prod\" \"pnpm --filter=workers start:prod\" \"pnpm -C apps/web start:prod\" \"pnpm --filter=pwa start:prod\"",
    "check": "turbo check --filter=!web",
    "lint": "turbo lint --filter=!web",
    "test": "turbo run test",
    "test:watch": "turbo run test:watch",
    "build:auth": "turbo build --filter=auth",
    "build:db": "turbo build --filter=db",
    "build:models": "turbo build --filter=models",
    "build:otel": "turbo build --filter=otel",
    "build:packages": "turbo build --concurrency=1 --filter='./packages/*'",
    "build:services": "turbo build --filter=services",
    "build:shared": "turbo build --filter=shared",
    "build:rpc": "turbo build --filter=rpc",
    "build:queue": "turbo build --filter=queue",
    "db:generate": "turbo db:generate",
    "db:migrate": "turbo db:migrate",
    "db:push": "turbo db:push"
  },
  "devDependencies": {
    "@types/qrcode": "^1.5.6",
    "concurrently": "^9.1.0",
    "sharp": "^0.34.5",
    "turbo": "^2.3.0",
    "vitest": "^3.2.4"
  },
  "packageManager": "pnpm@10.32.1",
  "engines": {
    "node": ">=24"
  }
}
