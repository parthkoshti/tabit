{
  "name": "tabit",
  "private": true,
  "scripts": {
    "build": "turbo build --concurrency=1 --filter=!web",
    "build:data": "turbo build --filter=data",
    "generate-icons": "node scripts/generate-icons.js",
    "generate-https-certs": "node scripts/generate-https-certs.js",
    "dev": "turbo dev --filter=!web",
    "dev:web": "cd apps/web && pnpm dev",
    "dev:pwa": "turbo dev --filter=pwa",
    "start": "pnpm --filter=pwa start",
    "start:web": "cd apps/web && pnpm start",
    "start:pwa": "pnpm --filter=pwa start",
    "start:api": "pnpm --filter=api start",
    "start:notifications": "pnpm --filter=notifications start",
    "start:all": "concurrently -n api,notifications,web,pwa -c blue,green,magenta,yellow \"pnpm --filter=api start\" \"pnpm --filter=notifications start\" \"cd apps/web && pnpm start\" \"pnpm --filter=pwa start\"",
    "start:prod": "pnpm --filter=db db:migrate:prod && concurrently -n api,notifications,web,pwa -c blue,green,magenta,yellow \"pnpm --filter=api start\" \"pnpm --filter=notifications start\" \"pnpm -C apps/web start:prod\" \"pnpm --filter=pwa start:prod\"",
    "check": "turbo check --filter=!web",
    "lint": "turbo lint --filter=!web",
    "test": "turbo run test",
    "test:watch": "turbo run test:watch",
    "build:auth": "turbo build --filter=auth",
    "build:db": "turbo build --filter=db",
    "build:models": "turbo build --filter=models",
    "build:otel": "turbo build --filter=otel",
    "build:packages": "turbo build --concurrency=1 --filter='./packages/*'",
    "build:services": "turbo build --filter=services",
    "build:shared": "turbo build --filter=shared",
    "db:generate": "turbo db:generate",
    "db:migrate": "turbo db:migrate",
    "db:push": "turbo db:push",
    "docker:build": "docker build --build-arg TURBO_TEAM=$TURBO_TEAM --build-arg TURBO_TOKEN=$TURBO_TOKEN -t tabit .",
    "docker:run": "docker run -p 3000:3000 -p 3002:3002 --env-file .env tabit"
  },
  "devDependencies": {
    "@types/qrcode": "^1.5.6",
    "concurrently": "^9.1.0",
    "sharp": "^0.34.5",
    "turbo": "^2.3.0",
    "vitest": "^3.2.4"
  },
  "packageManager": "pnpm@10.32.1",
  "engines": {
    "node": ">=24"
  }
}
