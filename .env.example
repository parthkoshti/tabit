DATABASE_URL=postgresql://user:password@localhost:5432/tab
NODE_ENV=development

# Web landing (tabit.in)
NEXT_PUBLIC_WEB_URL=http://localhost:3000
NEXT_PUBLIC_PWA_URL=https://localhost:3003
NEXT_PUBLIC_DONATE_URL=https://paypal.me/parthk

# API (api.tabit.in) — oRPC at /rpc, Better Auth at /api/auth
VITE_API_URL=http://localhost:3001
# Realtime workers (wrk.tabit.in) — Socket.IO
VITE_REALTIME_URL=http://localhost:3002

# Legacy aliases (avoid in new code)
# VITE_BACKEND_URL=
# VITE_NOTIFICATIONS_WS_URL=

BETTER_AUTH_SECRET=your_secret_min_32_chars_long
BETTER_AUTH_COOKIE_DOMAIN=
BETTER_AUTH_TRUSTED_ORIGINS=http://localhost:3003,http://localhost:3001

PLUNK_SECRET_KEY=your_plunk_secret_key
PLUNK_BASE_URL=https://plunk.example.com/api

DISCORD_WEBHOOK_URL=

REDIS_URL=redis://localhost:6379

VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VITE_VAPID_PUBLIC_KEY=

CORS_ORIGIN=http://localhost:3003

GOOGLE_GENERATIVE_AI_API_KEY=

# Doppler (optional locally — `doppler setup` per app)
# Cloud compose: DOPPLER_TOKEN_API / _WORKERS are runtime; DOPPLER_TOKEN_PWA is a PWA build arg.
# DOPPLER_TOKEN_API=
# DOPPLER_TOKEN_WORKERS=
# DOPPLER_TOKEN_PWA=

OTEL_EXPORTER_OTLP_ENDPOINT=
OTEL_EXPORTER_OTLP_HEADERS=
OTEL_SERVICE_NAME=
OTEL_SDK_DISABLED=

TURBO_TEAM=
TURBO_TOKEN=
